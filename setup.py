import setuptools

setuptools.setup(
    name='twix-anonymizer',    # This is the name of your PyPI-package.
    version='0.1',                          # Update the version number for new releases
    packages=setuptools.find_packages(),                  # The name of your scipt, and also the command you'll be using for calling it
)